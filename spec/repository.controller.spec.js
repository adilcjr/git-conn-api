// const controller = require('./repository.controller');

// describe ("Get repositories", function() {
//     const rootPath = '/';
//     const result = controller(rootPath);

//     if (result.endsWith('\"}]')) {
//         console.log('Controller is returning this:' + result);
//     } else {
//         console.error('Controller is not returning the wrong answer');
//     }
// });

